using UnityEngine;

public class TechUpgradeManager : MonoBehaviour
{
    public TechUpgradeManager instance { get; private set; }

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
